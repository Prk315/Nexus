[package]
name = "nexus-core"
version = "0.1.0"
edition = "2021"
description = "Shared types, errors, and utilities for Nexus applications"

[lib]
name = "nexus_core"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
