{
  "name": "@nexus/job-applier",
  "private": true,
  "type": "module",
  "description": "Job-post extraction for the n8n job applier. Deliberately outside the npm workspace globs (apps/*, packages/*) so it has no dependencies and cannot break an app build.",
  "scripts": {
    "test": "node --test extract.test.js"
  }
}
