/**
 * Barrel for the PathFinder tasks feature. `TasksPage` is a full page mounted
 * from `App.tsx` when its `page` state is `"tasks"` — same shape as
 * `lib/learn`'s `LearnPage`. Panels/components register here, never in
 * `App.tsx`.
 */

export { TasksPage } from "./TasksPage";
